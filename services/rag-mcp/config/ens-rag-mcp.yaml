server:
  host: "0.0.0.0"
  port: 8000

supabase:
  url_env: "SUPABASE_URL"
  service_role_key_env: "SUPABASE_SERVICE_ROLE_KEY"

policy:
  common_tenant: "ens"
  admin_profiles:
    - "ceo"
    - "default"
  default_limit: 8
  max_limit: 20

